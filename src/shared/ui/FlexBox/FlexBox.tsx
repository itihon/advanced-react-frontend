import React from 'react';

interface FlexBoxProps extends Pick<CSSStyleDeclaration, 'flexDirection' | 'alignItems' | 'alignContent' | 'jus'> {
 /*
  direction
  align
  justify
  gap
 */  
}

const FlexBox: React.FC<FlexBoxProps> = ({  }) => {
  return (
    <></>
  );
};

export default FlexBox;